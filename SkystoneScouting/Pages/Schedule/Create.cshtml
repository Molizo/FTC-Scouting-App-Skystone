@page
@model SkystoneScouting.Pages.Schedule.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ScheduledMatch</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ScheduledMatch.Red1TeamID" class="control-label"></label>
                <select asp-for="ScheduledMatch.Red1TeamID">
                    <option value="null">Select team</option>
                    @foreach (var Team in Model.AuthorizedTeams)
                    {
                        <option value="@Team.ID">@Team.TeamNumber | @Team.TeamName</option>
                    }
                </select>
                <span asp-validation-for="ScheduledMatch.Red1TeamID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ScheduledMatch.Red2TeamID" class="control-label"></label>
                <select asp-for="ScheduledMatch.Red2TeamID">
                    <option value="null">Select team</option>
                    @foreach (var Team in Model.AuthorizedTeams)
                    {
                        <option value="@Team.ID">@Team.TeamNumber | @Team.TeamName</option>
                    }
                </select>
                <span asp-validation-for="ScheduledMatch.Red2TeamID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ScheduledMatch.Blue1TeamID" class="control-label"></label>
                <select asp-for="ScheduledMatch.Blue1TeamID">
                    <option value="null">Select team</option>
                    @foreach (var Team in Model.AuthorizedTeams)
                    {
                        <option value="@Team.ID">@Team.TeamNumber | @Team.TeamName</option>
                    }
                </select>
                <span asp-validation-for="ScheduledMatch.Blue1TeamID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ScheduledMatch.Blue2TeamID" class="control-label"></label>
                <select asp-for="ScheduledMatch.Blue2TeamID">
                    <option value="null">Select team</option>
                    @foreach (var Team in Model.AuthorizedTeams)
                    {
                        <option value="@Team.ID">@Team.TeamNumber | @Team.TeamName</option>
                    }
                </select>
                <span asp-validation-for="ScheduledMatch.Blue2TeamID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ScheduledMatch.RedScore" class="control-label"></label>
                <input asp-for="ScheduledMatch.RedScore" class="form-control" />
                <span asp-validation-for="ScheduledMatch.RedScore" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ScheduledMatch.BlueScore" class="control-label"></label>
                <input asp-for="ScheduledMatch.BlueScore" class="form-control" />
                <span asp-validation-for="ScheduledMatch.BlueScore" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}